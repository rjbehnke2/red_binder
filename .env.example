# Red Binder — Environment Variables
# Copy to .env.local and fill in your values

# Supabase (Week 1 — add after scaffold is verified)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Anthropic (Week 5 — stored in Supabase Edge Function secrets, NOT here)
# ANTHROPIC_API_KEY is set in Supabase dashboard → Edge Functions → Secrets
